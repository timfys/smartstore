@{
    Assets.BodyAttributes.AppendCssClass("home");
    Assets.AppendTitleParts((string)ViewBag.MetaTitle);
    Assets.AppendMetaDescriptionParts((string)ViewBag.MetaDescription);
    Assets.AppendMetaKeywordsParts((string)ViewBag.MetaKeywords);
}

<div class="page home-page">
    <div class="page-body">
        <zone name="home_page_top" />
        <button class="scroll-top" aria-label="Back to top">
            <svg viewBox="0 0 24 24" aria-hidden="true">
                <path d="M12 5l-7 7h4v7h6v-7h4z"/>
            </svg>
        </button>
        <section class="hero">
            <div class="hero-content">
                <div class="brand">
                    <div class="brand-logo"></div>
                    <div class="brand-title">
                        Oksana Miller<br>Design Studio
                    </div>
                </div>

                <p class="description">
                    Unique handmade bags created with precision, elegance,
                    and individuality
                </p>

                <a class="btn" href="#collections-section">View Collections</a>

            </div>
        </section>
        <section class="brand-links">
            <a href="#collections-section" class="brand-card">
                <img src="images/collections.png" alt="Collections">
                <span>COLLECTIONS</span>
            </a>

            <a href="#about-brand" class="brand-card">
                <img src="images/about.png" alt="About the brand">
                <span>ABOUT THE BRAND</span>
            </a>

            <a href="contactus" class="brand-card">
                <img src="images/contacts.png" alt="Contacts">
                <span>CONTACTS</span>
            </a>
        </section>
        <section class="collections-section" id="collections-section">
            <h2>COLLECTIONS</h2>

            <div class="collections-category">
                <span class="collections-label left">Classic models</span>
                <div class="collections-static__grid">
                    <a href="/bag1" class="collection-item">
                        <img src="images/bag-1.png" alt="Classic bag 1">
                    </a>

                    <a href="/bag2" class="collection-item">
                        <img src="images/bag-2.png" alt="Classic bag 2">
                    </a>

                    <a href="/bag3" class="collection-item">
                        <img src="images/bag-3.png" alt="Classic bag 3">
                    </a>
                </div>

            </div>

           @* <div class="collections-category">
                <span class="collections-label right">Everyday bags</span>

                <div class="collections-grid">
                    @await Component.InvokeAsync("HomeProducts")
                </div>
            </div>
            <div class="collections-category">
                <span class="collections-label left">Evening clutches</span>

                <div class="collections-grid">
                    @await Component.InvokeAsync("HomeProducts")
                </div>
            </div>
            <div class="collections-category">
                <span class="collections-label right">Mini bags & backpacks</span>

                <div class="collections-grid">
                    @await Component.InvokeAsync("HomeProducts")
                </div>
            </div>
            <div class="collections-category">
                <span class="collections-label left">Exclusive orders</span>

                <div class="collections-grid">
                    @await Component.InvokeAsync("HomeProducts")
                </div>
            </div>*@
        </section>
        
        <section class="about-brand" id="about-brand">
            <div class="about-brand__top">
                <div class="about-brand__text">
                    <h2>ABOUT THE BRAND</h2>
                    <p>
                        Each handbag is born in our atelier, crafted by hand with attention to
                        detail and individuality at its heart. We create unique pieces that
                        reflect the personality and style of their owner.
                    </p>
                </div>

                <div class="about-brand__image">
                    <img src="images/about-main.png" alt="Oksana Miller Design Studio">
                </div>
            </div>

            <div class="about-brand__gallery">
                <div class="about-brand__item">
                    <img src="images/about-look-1.png" alt="Handbag look 1">
                </div>
                <div class="about-brand__item">
                    <img src="images/about-look-2.png" alt="Handbag look 2">
                </div>
                <div class="about-brand__item">
                    <img src="images/about-look-3.png" alt="Handbag look 3">
                </div>
            </div>
        </section>
        <section class="cta-banner">
            <div class="cta-banner__image cta-banner__image--left">
                <img src="images/bag-left.png" alt="Handcrafted bag">
            </div>

            <div class="cta-banner__content">
                <h2>Discover Your Exclusive Style</h2>
                <p>
                    Order your handcrafted bag today or request a bespoke design tailored to you
                </p>

                <a href="contactus" class="cta-banner__button">CONTACT US</a>

                <div class="cta-banner__contacts">
                    <a href="tel:+972542792829" class="contact-link">
                        <svg viewBox="0 0 24 24" aria-hidden="true">
                            <path d="M6.6 10.8c1.5 3 3.6 5.1 6.6 6.6l2.2-2.2c.3-.3.7-.4 1.1-.3
      1.2.4 2.6.6 4 .6.6 0 1 .4 1 1V21c0 .6-.4 1-1 1
      C10.1 22 2 13.9 2 3c0-.6.4-1 1-1h3.5c.6 0 1 .4 1 1
      0 1.4.2 2.8.6 4 .1.4 0 .8-.3 1.1l-2.2 2.2z"/>
                        </svg>
                        <span>+972 54-279-2829</span>
                    </a>

                    <a href="mailto:oksanag88@gmail.com" class="contact-link">
                        <svg viewBox="0 0 24 24" aria-hidden="true">
                            <path d="M20 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16
      c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4-8 5-8-5V6l8 5
      8-5v2z"/>
                        </svg>
                        <span>oksanag88@gmail.com</span>
                    </a>

                    <a href="https://instagram.com/oksanamiller_studio"
                       target="_blank"
                       class="contact-link">
                        <svg viewBox="0 0 24 24" aria-hidden="true">
                            <path d="M7 2h10a5 5 0 015 5v10a5 5 0 01-5 5H7a5 5 0
      01-5-5V7a5 5 0 015-5zm10 2H7a3 3 0 00-3 3v10a3 3 0
      003 3h10a3 3 0 003-3V7a3 3 0 00-3-3z"/>
                            <circle cx="12" cy="12" r="3.2"/>
                            <circle cx="17.5" cy="6.5" r="0.8"/>
                        </svg>
                        <span>oksanamiller_studio</span>
                    </a>
                </div>
            </div>

            <div class="cta-banner__image cta-banner__image--right">
                <img src="images/bag-right.png" alt="Designer bag">
            </div>
        </section>
        @*@await Component.InvokeAsync("TopicBlock", new { systemName = "HomePageText", isLead = true })
        <zone name="home_page_after_intro" />

        @await Component.InvokeAsync("HomeCategories")
        <zone name="home_page_after_categories" />

        @await Component.InvokeAsync("HomeProducts")
        <zone name="home_page_after_products" />

        @await Component.InvokeAsync("HomeBestsellers")
        <zone name="home_page_after_bestsellers" />

        @await Component.InvokeAsync("HomeBrands")
        <zone name="home_page_after_manufacturers" />

        @await Component.InvokeAsync("PopularProductTags")
        <zone name="home_page_after_tags" />*@

        @* TODO: (mc) find more intelligent way of rendering RecentlyViewedProducts globally *@
        @*@await Component.InvokeAsync("RecentlyViewedProducts")*@

        <zone name="home_page_bottom" />
    </div>
</div>
<style>
    div {
        scroll-behavior: smooth;
    }
    .hero {
        width: 100vw;
        max-width: 100vw;
        margin-left: calc(50% - 50vw);
        margin-right: calc(50% - 50vw);
        margin-top: 0;

        min-height: 80vh;
        background:
            linear-gradient(
                to right,
                rgba(0,0,0,0.75) 40%,
                rgba(0,0,0,0.55) 70%,
                rgba(0,0,0,0.75) 100%
            ),
            url("images/image_179.png") left / cover no-repeat;
        background-position: left center;
        background-size: cover; 
        display: flex;
        align-items: center;
        padding: 80px;
    }

    .hero-content {
        max-width: 520px;
        color: #f2f2f2;
        margin-left: auto;
    }

    .brand {
        display: flex;
        align-items: center;
        gap: 16px;
        margin-bottom: 28px;
    }

    .brand-logo {
        width: 52px;
        height: 52px;
        border: 1.5px solid #cfcfcf;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 18px;
        letter-spacing: 1px;
        background: url("images/logo_big.png") left / cover no-repeat;
    }

    .brand-title {
        font-size: 34px;
        letter-spacing: 3px;
        text-transform: uppercase;
        line-height: 1.2;
    }

    .description {
        font-size: 16px;
        line-height: 1.6;
        color: #d6d6d6;
        margin-bottom: 36px;
        max-width: 420px;
    }

    .btn {
        padding: 14px 34px;
        border: 1px solid #d6d6d6;
        background: transparent;
        color: #fff;
        text-transform: uppercase;
        letter-spacing: 1px;
        font-size: 13px;
        cursor: pointer;
        transition: all 0.3s ease;
    }

    .btn:hover {
        background: #fff;
        color: #000;
    }

    @@media (max-width: 900px) {
    
        .hero-content {
            margin-left: 0;
            margin-bottom: 430px;
        }
    
        .brand-title {
            font-size: 26px;
        }  
        .hero {
            padding: 60px 30px;

            min-height: 100vh;

            background-image:
                linear-gradient(
                    to bottom,
                    rgba(0,0,0,0.55) 0%,
                    rgba(0,0,0,0.65) 60%,
                    rgba(0,0,0,0.85) 100%
                ),
                url("images/image_215.png");

            background-size: cover;
            background-position: center top;
            background-repeat: no-repeat;
        }
        .brand {
            display: flex;
            align-items: center;
            gap: 16px;
            margin-bottom: 48px;
        }
    }
    .brand-links {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 40px;
        max-width: 1200px;
        margin: 0 auto;
        padding: 80px 20px;
    }

    .brand-card {
        text-decoration: none;
        color: #000;
        text-align: center;
        transition: transform 0.3s ease;
    }

    .brand-card img {
        width: 100%;
        aspect-ratio: 4 / 5;
        object-fit: cover;
        display: block;
    }

    .brand-card span {
        display: block;
        margin-top: 24px;
        font-size: 22px;
        letter-spacing: 0.08em;
        font-weight: 400;
    }

    .brand-card:hover {
        transform: translateY(-6px);
    }

    /* Адаптив */
    @@media (max-width: 900px) {
    .brand-links {
        grid-template-columns: 1fr;
        gap: 60px;
    }
    }
    .cta-banner {
        position: relative;
        display: grid;
        grid-template-columns: 1fr minmax(320px, 690px) 1fr;
        align-items: center;
        min-height: 480px;
        padding: 40px 20px;
        background: radial-gradient(circle at center, #1a1a1a 0%, #000 70%);
        color: #fff;
        overflow: hidden;
    }

    /* Images */
    .cta-banner__image {
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .cta-banner__image img {
        max-width: 120%;
        max-height: 420px;
        object-fit: contain;
    }
    .cta-banner__image--right img {
        max-width: 120%;
        max-height: 420px;
        object-fit: contain;
    }

    /* Content */
    .cta-banner__content {
        text-align: center;
        z-index: 2;
    }

    .cta-banner__content h2 {
        font-family: "Playfair Display", serif;
        font-size: 56px;
        font-weight: 500;
        margin-bottom: 24px;
    }

    .cta-banner__content p {
        font-size: 22px;
        line-height: 1.5;
        margin-bottom: 40px;
    }

    /* Button */
    .cta-banner__button {
        display: inline-block;
        padding: 18px 48px;
        border-radius: 14px;
        border: 1px solid #fff;
        background: rgba(255, 255, 255, 0.15);
        color: #fff;
        text-decoration: none;
        font-size: 18px;
        letter-spacing: 0.08em;
        transition: all 0.3s ease;
    }

    .cta-banner__button:hover {
        background: #fff;
        color: #000;
    }

    /* Contacts */
    .cta-banner__contacts {
        margin-top: 48px;
        display: flex;
        justify-content: center;
        gap: 40px;
        font-size: 16px;
        opacity: 0.9;
    }

    .cta-banner__contacts a {
        color: #fff;
        text-decoration: none;
    }

    .cta-banner__contacts a:hover {
        text-decoration: underline;
    }

    .contact-link {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        color: #fff;
        text-decoration: none;
        opacity: 0.9;
    }

    .contact-link svg {
        width: 16px;
        height: 16px;
        fill: none;
        stroke: currentColor;
        stroke-width: 1.7;
    }

    .contact-link:hover {
        opacity: 1;
    }
    /* Adaptive */
    @@media (max-width: 1000px) {
    .cta-banner {
        grid-template-columns: 1fr;
    }

    .cta-banner__image {
        display: none;
    }

    .cta-banner__content h2 {
        font-size: 40px;
    }

    .cta-banner__content p {
        font-size: 18px;
    }

    .cta-banner__contacts {
        flex-direction: column;
        gap: 16px;
    }
    }
    .about-brand {
        max-width: 1200px;
        margin: 0 auto;
        padding: 100px 20px;
    }

    /* Top block */
    .about-brand__top {
        display: grid;
        grid-template-columns: 1fr 1.2fr;
        gap: 60px;
        align-items: start;
        margin-bottom: 80px;
    }
    .collections-title h2,
    .about-brand__text h2 {
        font-family: "Playfair Display", serif;
        font-size: 36px;
        letter-spacing: 0.08em;
        margin-bottom: 32px;
    }

    .about-brand__text p {
        font-size: 16px;
        line-height: 1.7;
        max-width: 420px;
    }

    .about-brand__image img {
        width: 100%;
        height: auto;
        display: block;
    }

    /* Bottom gallery */
    .about-brand__gallery {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 32px;
    }

    .about-brand__item img {
        width: 100%;
        aspect-ratio: 3 / 4;
        object-fit: cover;
        display: block;
    }

    /* Adaptive */
    @@media (max-width: 1000px) {
    .about-brand__top {
        grid-template-columns: 1fr;
        gap: 40px;
    }

    .about-brand__text p {
        max-width: 100%;
    }

    .about-brand__gallery {
        grid-template-columns: 1fr;
        gap: 24px;
    }
    }
    .scroll-top {
        position: fixed;
        right: 28px;
        bottom: 28px;
        width: 44px;
        height: 44px;
        border-radius: 50%;
        border: 1px solid rgba(255,255,255,0.6);
        background: rgba(0,0,0,0.55);
        color: #fff;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;

        opacity: 0;
        visibility: hidden;
        transition: opacity 0.3s ease, visibility 0.3s ease, transform 0.3s ease;
        z-index: 999;
    }

    .scroll-top svg {
        width: 18px;
        height: 18px;
        fill: currentColor;
    }

    .scroll-top:hover {
        background: #fff;
        color: #000;
        transform: translateY(-3px);
    }

    /* показываем */
    .scroll-top.is-visible {
        opacity: 1;
        visibility: visible;
    }

    /* mobile */
    @@media (max-width: 768px) {
    .scroll-top {
        right: 18px;
        bottom: 18px;
        width: 40px;
        height: 40px;
    }
    }
    .collections-section {
        max-width: 1200px;
        margin: 0 auto;
        padding: 100px 20px;
    }

    .collections-header {
        position: relative;
        margin-bottom: 40px;
    }

    .collections-header h2 {
        font-family: "Playfair Display", serif;
        font-size: 36px;
        letter-spacing: 0.08em;
        margin-bottom: 20px;
    }

    .collections-labels {
        position: absolute;
        top: 80px;
        width: 100%;
        display: flex;
        justify-content: space-between;
        font-size: 13px;
        opacity: 0.7;
    }
    .collections-label {
        position: absolute;
        top: 40%;
        font-size: 13px;
        opacity: 0.7;
    }
    .collections-label.left {
        left: -90px;
    }

    .collections-label.right {
        right: -90px;
    }
    .collections-grid .product-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 40px;
    }
    .collections-section h2 {
        font-family: "Playfair Display", serif;
        font-size: 36px;
        letter-spacing: 0.08em;
        font-weight: 400;
    }
    .collections-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 40px;
    }

    /* карточки товаров SmartStore */
    .collections-grid .product-item {
        border: none;
        text-align: center;
    }

    .collections-grid .picture img {
        width: 100%;
        aspect-ratio: 1 / 1;
        object-fit: cover;
    }
    .collections-category {
        position: relative;
        margin-bottom: 60px;
    }
    .collection-item {
        display: block;
        overflow: hidden;
    }

    .collection-item img {
        width: 100%;
        aspect-ratio: 1 / 1;
        object-fit: cover;
        display: block;
        transition: transform 0.4s ease;
    }

    .collection-item:hover img {
        transform: scale(1.04);
    }
    .collections-static__grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr); /* ← ВАЖНО */
        gap: 40px;
        width: 100%;
    }
    /* Mobile */
    @@media (max-width: 900px) {
    .collections-grid {
        grid-template-columns: 1fr;
    }

    .collections-labels {
        position: static;
        margin-bottom: 20px;
    }
    }
</style>

<script>
  document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
      const target = document.querySelector(this.getAttribute('href'));
      if (!target) return;

      e.preventDefault();
      target.scrollIntoView({
        behavior: 'smooth',
        block: 'start'
      });
    });
  });
  const scrollTopBtn = document.querySelector('.scroll-top');
  const scrollContainer = document.querySelector('.page-body') || window;

  // показываем стрелку
  scrollContainer.addEventListener('scroll', () => {
      const scrollTop = scrollContainer === window
          ? window.scrollY
          : scrollContainer.scrollTop;

      if (scrollTop > 400) {
          scrollTopBtn.classList.add('is-visible');
      } else {
          scrollTopBtn.classList.remove('is-visible');
      }
  });

  // плавно наверх
  scrollTopBtn.addEventListener('click', () => {
      if (scrollContainer === window) {
          window.scrollTo({ top: 0, behavior: 'smooth' });
      } else {
          scrollContainer.scrollTo({ top: 0, behavior: 'smooth' });
      }
  });   
</script>