<section class="site-footer">
    <div class="site-footer__top">
        <div class="footer-brand">
            <div class="footer-logo"></div>
            <div class="footer-brand-text">
                OKSANA MILLER<br>
                DESIGN STUDIO
            </div>
        </div>
    </div>

    <div class="site-footer__middle">
        <nav class="footer-nav">
            <a href="/#about-brand">About</a>
            <a href="/#collections-section">Collection</a>
            <a href="/#collections-section">Showcase</a>
        </nav>

        <div class="footer-contacts">
            <a href="tel:+972542792829">
                <svg viewBox="0 0 24 24"><path d="M6.6 10.8c1.5 3 3.6 5.1 6.6 6.6l2.2-2.2c.3-.3.7-.4 1.1-.3
        1.2.4 2.6.6 4 .6.6 0 1 .4 1 1V21c0 .6-.4 1-1 1
        C10.1 22 2 13.9 2 3c0-.6.4-1 1-1h3.5c.6 0 1 .4 1 1
        0 1.4.2 2.8.6 4 .1.4 0 .8-.3 1.1l-2.2 2.2z"/></svg>
                <span>+972 54-279-2829</span>
            </a>

            <a href="mailto:oksanag88@gmail.com">
                <svg viewBox="0 0 24 24"><path d="M20 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16
        c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4-8 5-8-5V6l8 5
        8-5v2z"/></svg>
                <span>oksanag88@gmail.com</span>
            </a>

            <a href="https://instagram.com/oksanamiller_studio" target="_blank">
                <svg viewBox="0 0 24 24">
                    <path d="M7 2h10a5 5 0 015 5v10a5 5 0 01-5 5H7a5 5 0
          01-5-5V7a5 5 0 015-5z"/>
                    <circle cx="12" cy="12" r="3.2"/>
                    <circle cx="17.5" cy="6.5" r="0.8"/>
                </svg>
                <span>oksanamiller_studio</span>
            </a>
        </div>
    </div>

    <div class="site-footer__bottom">
        <p>© 2025 Oksana Miller Design Studio. All rights reserved.</p>
        <p>Handcrafted bags made with passion and individuality.</p>
    </div>
</section>
<style>
    .site-footer {
        background: #2e2e2e;
        color: #e6e6e6;
        padding: 60px 20px 40px;
        font-size: 14px;
    }

    /* TOP */
    .site-footer__top {
        display: flex;
        justify-content: center;
        margin-bottom: 40px;
    }

    .footer-brand {
        display: flex;
        align-items: center;
        gap: 16px;
    }

    .footer-logo {
        width: 56px;
        height: 56px;
        border: 1.5px solid #aaa;
        border-radius: 50%;
        background: url("images/logo_big.png") center / cover no-repeat;
    }

    .footer-brand-text {
        text-transform: uppercase;
        font-size: 12px;
        letter-spacing: 2px;
        line-height: 1.4;
    }

    /* MIDDLE */
    .site-footer__middle {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 32px;
        margin-bottom: 40px;
    }

    .footer-nav {
        display: flex;
        gap: 80px;
    }

    .footer-nav a {
        color: #e6e6e6;
        text-decoration: none;
        letter-spacing: 1px;
        font-size: 13px;
    }

    .footer-nav a:hover {
        opacity: 0.7;
    }

    .footer-contacts {
        display: flex;
        flex-direction: column;
        gap: 14px;
        align-items: center;
    }

    .footer-contacts a {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        color: #e6e6e6;
        text-decoration: none;
        opacity: 0.9;
    }

    .footer-contacts svg {
        width: 15px;
        height: 15px;
        fill: none;
        stroke: currentColor;
        stroke-width: 1.6;
    }

    /* BOTTOM */
    .site-footer__bottom {
        font-size: 11px;
        opacity: 0.7;
        display: flex;
        justify-content: space-between;
        max-width: 1200px;
        margin: 0 auto;
    }

    /* MOBILE */
    @@media (max-width: 768px) {
    .footer-nav {
        gap: 32px;
        flex-direction: column;
        align-items: center;
    }

    .site-footer__bottom {
        flex-direction: column;
        gap: 6px;
        text-align: center;
    }
    }
</style>
<script>
    document.querySelectorAll('a[href*="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            const hash = this.hash;
            if (!hash) return;

            const target = document.querySelector(hash);
            if (!target) return;

            e.preventDefault();

            target.scrollIntoView({
                behavior: 'smooth',
                block: 'start'
            });

            // обновляем URL без перезагрузки
            history.pushState(null, '', hash);
        });
    });
</script>